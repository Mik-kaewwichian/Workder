import { View, Text } from 'react-native';

export default function SafeZone() {
  return (
    <View style={{ flex: 1, padding: 24 }}>
      <Text style={{ fontSize: 18, fontWeight: '700' }}>Safe Zone</Text>
    </View>
  );
}
